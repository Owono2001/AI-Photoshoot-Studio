Flask
Flask-Session
opencv-python-headless
Pillow
numpy
python-dotenv
gunicorn # For deployment
mediapipe >= 0.9.0 # For virtual background
tensorflow >= 2.8.0 # For style transfer (can be large!)
tensorflow_hub >= 0.12.0 # For loading pre-trained TF models
scikit-image # Often useful for image tasks, blending
gunicorn
# Fabric.js is a frontend library, included via CDN in HTML